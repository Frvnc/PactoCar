# ============================================================
# Copia este archivo como .env y completa los valores reales.
# NUNCA subas el .env al repositorio.
# ============================================================

# En desarrollo local apunta al backend en Docker:
VITE_API_URL=http://localhost:3000

# En producción (AWS/Azure) usa la URL pública del backend:
# VITE_API_URL=https://api.pactocar.com
